notifications:
  notification_slack:
    config:
      botId: "{{.NOTIFICATIONS_SLACK_BOT_ID}}"
      hookId: "{{.NOTIFICATIONS_SLACK_HOOK_ID}}"
      teamId: "{{.NOTIFICATIONS_SLACK_TEAM_ID}}"
    driver: slack
secretsFile: ./notifications.secrets.yml
