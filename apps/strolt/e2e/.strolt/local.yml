services:
  e2e:
    local:
      destinations:
        restic-local:
          env:
            RESTIC_REPOSITORY: "s3:http://minio:9000/restic-local"
          extends: restic_base
      source:
        config:
          path: "/e2e/input"
        driver: local
