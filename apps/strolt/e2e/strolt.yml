api:
  users:
    admin: "{{.API_ADMIN_PASSWORD}}"

extends:
  configs:
    - .strolt/base.yml

    - .strolt/local.yml
    - .strolt/prune.yml

    - .strolt/pg.yml
    - .strolt/mongo.yml
    # - .strolt/mysql.yml
    - .strolt/mariadb.yml
  secrets:
    - .strolt/secrets.yml
name: strolt-e2e
timezone: UTC
